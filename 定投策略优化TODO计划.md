# 行业周K定投策略优化 TODO 计划（V6 → V7）

> 制定日期：2026-09-26。
> 依据：现行 V6 规则审查；《策略vs买入持有对比报告.md》（稳健参数版本均跑输买入持有）；AMV 择时研究结论（熊市保险型过滤器，诚实预期年化 13-16%、回撤 -30% 级）。
> 优化定位：**不追求提高年化，追求降低行业定投的尾部风险**（价值陷阱、熊市满仓、止盈回吐）。

> ## ⚡ 执行结果（2026-09-26，Phase 0-3 已全部完成）
> **预注册采纳标准判定：全部候选配置未通过 → 生产保持 V6，Phase 4 灰度不启动。**
> 行业ETF组（9只）池化：OOS Calmar 改善全部为负（−10%～−45%）、规则2通过数 0、全窗口 Calmar/利润全线低于 V6；
> 敏感性检验显示结论是系统性的。V7 代码、AMV 闸门、验证运行器全部保留可复现。
> 详见 **《V7_vs_V6_对比报告.md》** 与 `results/v7/`、`html/v7_compare/V7_vs_V6_对比报告.html`。
> 有价值的记录：T4B(j20) 最接近 V6；`j20+T1freeze+T3+AMV(阈值16)` 组合池化 Calmar 0.065 为**事后观察**，只可注册为新候选后重测，不得直接采纳。

## 总原则（验收纪律，先于任何开发定死）

1. 验收基准 = 现行 V6 **同现金流对比**，不与买入持有比（定投是现金流执行框架，满仓对比不公平）。
2. 目标函数 = Calmar / 最大回撤 / 资金曲线平滑优先；年化仅作参考，不作为采纳依据。
3. 任何新参数先过 ±20% 敏感性 + IS/OOS（前 70% 训练 / 后 30% 验证）；IS 有效 OOS 失效 = 过拟合，弃。
4. 所有特性做成独立开关，逐项消融，不允许组合打包上线。
5. 未达采纳标准 → 保持 V6，研究归档并记录否决原因（防止将来重复研究）。

---

## Phase 0 工程卫生（先行，约半天）✅ 已完成

- [x] **P0-1** `to_dict`/`from_dict` 补 `cycle_low`、`j_high_done` 两个字段（`weekly_dca_strategy.py:305-354`）。V4 起用 `getattr` 读取（`weekly_dca_strategy_v4.py:194-201`），断点续跑路径会把 J 门控和创新低止损重置。当前生产是每次全量重放所以决策无恙，但任何增量决策路径都会踩中。
- [x] **P0-2** 修 `weekly_dca_strategy.py:6-11` 文件头 docstring：还写着"J>100 卖半仓"旧规则，实际是 J≥93 卖 1/3。
- [x] **P0-3** 跑 `test_weekly_decision_consistency.py` 确认零回归；补一个"重启后决策一致"用例 → `test_p0_state_persistence.py`（4 项全过）。注意：v1/v2 各有一个 Position 类，两处都要补。

## Phase 1 V7 骨架 + 买入端三特性 ✅ 已完成（等价性 14/14 通过）

### T-V7 V7 骨架（0.5 天，一切开发的前置）

- [x] 新建 `weekly_dca_strategy_v7.py` 继承 V6，特性开关：`use_trend_gate` / `use_j_percentile` / `ramp_mode` / `use_amv_gate`（全部默认关闭 = 行为等价 V6）。
- [x] 状态文件前缀 `dca_state_v7_{name}.json`（参照 `weekly_dca_strategy_v6.py:52`）；V7 新增字段（gate_factor/ramp_stage）已进 to_dict。
- [x] `run_backtest_from_config` 加 v7 分支（`weekly_dca_strategy.py:1777-1785` 现在只认 v1/v6，else 分支会把 "v7" 当 sell_version 塞给 V1 类——必须加 elif，否则配置一改就静默走错策略类）。
- [x] 报告复用 `generate_backtest_report_v6`。
- [x] 回归验证：全开关关闭时，V7 与 V6 的交易序列逐笔一致（14/14 通过）。
- 过程记录：初版把 J>13 退出判断放进 `if remaining>0` 内部，导致预算被周投恰好耗尽的轮次永不退出、J门控永不解锁——等价性断言抓住了它。**等价性断言是 V7 类开发的必要门槛。**

### T4 回升投放节奏（0.5 天，改动最小，最先做）

- [x] 现规则（`weekly_dca_strategy_v4.py:176-187`）：J 回升 >13 即一次性打完剩余预算——熊市反弹里最伤。
- [x] 两个变体消融：**A**(split2)=剩余预算分 2 期投完；**B**(j20)=J>20 才允许一次打完。
- [x] 回测结论：两者均≈中性（行业组池化利润 18,510/19,158 vs V6 19,389）。j20 最接近 V6。

### T1 趋势资格闸门（1-2 天，解决行业价值陷阱）

- [x] 规则：周线收盘 < N 周均线（N=100 主参数，52 备选）或 N 周线下行 → **冻结新轮次**；已有轮次照常走卖出/止损规则。
- [x] 两个闸门形态消融：**A**(freeze)=冻结新轮；**B**(half)=新轮照开但预算减半。
- [x] 负反馈（补投）问题实测不存在：freeze 模式下轮次从未开立、预算从未承诺，现金留在账上（净值口径按已部署资金计），无需补投规则。
- [x] 检查各标的周线历史长度，N 周均线预热不足的 ETF 回退为"不启用闸门"并在报告标注（科创半导体/卫星/创业板新能源自动回退，验证正确）。
- [x] 回测结论：个案确有"熊市保险"形态（通信全窗口 DD −19.7%→−10.2% 且年化反升；上证50 DD −16.5%→−6.2%），但池化 OOS 回撤反而更宽、利润缩水——**未达标准**。

### T3 J 阈值分位化（1-2 天，一套参数管 14 个标的的修正）

- [x] `calc_kdj` 后加 rolling 156 周（3 年）分位：买入阈值 = 10 分位、过热卖出阈值 = 95 分位，逐标的自适应。
- [x] 历史 <156 周的次新 ETF 回退固定 13/93，报告标注。
- [x] 回测结论：**均值回复强的行业上过严**——买入频率减半（人工智能 51→26 次、有色 47→20 次），错过 V 型反弹，利润 −85%/−193%；低波组几乎无影响。未达标准。

## Phase 2 AMV 两票共振闸门（T2，有硬前置依赖）⚠️ 回测完成，生产前置未解决

- [x] **前置检查（阻塞项）**：`data/0AMV_day.csv` 在仓库内只有消费方（`amv_timing_backtest.py` / `amv_timing_etf.py` / `analyze_amv_corr.py`），**没有更新脚本**。确认为手工/外部维护——**此阻塞项仍未解决，T2 不得上线**。
- [x] 信号定义：市场级 AMV 两票——票 1 = AMV>MA10；票 2 = 多头共振（AMV>MA10 & MA10>MA30 & 成交额>20日均额）。≥1 票 = 闸门开。已抽取为 `amv_gate.py`（与原回测引擎逐日交叉验证一致）。
- [x] 与 T1 的分层：T1 是行业级资格、T2 是市场级仓位，两者独立叠加（都关 → 只投基础额；T1 关 → 冻结新轮）。
- [x] 信号计算复用 `amv_timing_etf.py` 口径；评估口径按诚实预期 13-16% 年化、-30% 级回撤。
- [x] 回测结论：在 T1/T3 之上叠加 AMV 一致地更差一点（边际拖累），与 AMV 研究定位一致（仓位中枢过滤器，不适合逐周微调）。未达标准。

## Phase 3 验证与采纳决策 ✅ 已完成（判定：不采纳）

- [x] 对比矩阵：V6 / V7OFF(等价性) / T4A / T4B / T1freeze / T1half / T3 / T2full，14 标的全量（`run_v7_verification.py`）。
- [x] IS/OOS：前 70% 训练、后 30% 验证；OOS 各项指标作为采纳依据。
- [x] 每特性 ±20% 参数敏感性热力图（16 变体 × 14 标的）。
- [x] **采纳标准（预注册，Phase 3 开始前写死，改标准=重开评审）**：候选组合在 OOS 上 Calmar 较 V6 改善 ≥15%；或最大回撤收窄 ≥10% 且年化损失 ≤2pp。两条都不满足 → 不采纳，保持 V6。
- [x] 产出《V7_vs_V6_对比报告.md》+ `html/v7_compare/V7_vs_V6_对比报告.html`；逐条记录被否决变体及原因。
- 执行注记：验证用自洽净值口径（市值+累计卖出金额)/累计投入；生产 `calc_nav_curve` 的（市值+已实现利润）口径漏加卖出返还本金，仅影响展示曲线，建议另行单独修正（本次未动）。

## Phase 4 灰度上线 ❌ 未启动（Phase 3 判定不通过）

- [ ] `etf_config.json` 选 2 只行业 ETF 灰度切 `strategy: "v7"`（建议创新药 + 科创半导体：前者是深度价值陷阱活样本，后者是高波代表）。**——判定不通过，本项取消；若将来采纳需重开评审。**
- [ ] 灰度观察 2-4 周：核对 `decision_ledger.json` 记录与邮件文案。
- [ ] 无异常 → 其余行业 ETF 切换；**宽基保持 V6**。
- [ ] 单独决策项（不在本计划内）：NASDAQ100 仍走 V1 带 ×2/×4 倍投，是否收敛到 V6/V7。

## 二期登记（本计划不含）

- [ ] T5 跨行业预算分配：每期预算按"J 分位深度 × 低位程度"加权分配到触发的 2-3 个行业，单行业同时最多 1 个活跃轮次；基准 = 分散均投。复用 `rotation_concentration.py` 的集中度结论。
- [ ] 低波行业补充止盈（trailing / 持有超 N 年且年化 >X% 的时间止盈）——若 T3 分位化已解决大半则取消此项。（本次验证显示 T3 未达标，此问题仍在，但 T3 本身不可采纳，需换思路重研究）
- [ ] `j20 + T1freeze + T3 + AMV(阈值16)` 组合：本次敏感性的**事后观察**（池化 Calmar 0.065 > V6 0.054），按纪律必须更换数据窗口重新注册为候选后才能研究，禁止用本次样本挑选参数。

## 风险登记

| 风险 | 影响 | 缓解 |
|---|---|---|
| 0AMV 数据无自动更新链路 | T2 无法上生产 | **已确认存在**；补齐前 T2 仅回测 |
| 次新 ETF 周线不足 3 年 | T3 分位数无参照 | 已实现自动回退固定阈值 + 报告标注 |
| 闸门冻结期错过 V 型反转 | T1 机会成本 | **实测兑现**：池化利润缩水 34%；OOS 回撤反而更宽 |
| 采纳标准事后放宽 | 过拟合复活 | 标准已预注册并严格执行（本次判定不通过即不采纳） |
| 灰度期与 V6 并存两套 state | 账目混乱 | V7 独立前缀 dca_state_v7_*，decision_ledger 按 strategy 字段区分 |

## 里程碑顺序

~~P0 → T-V7 骨架 → T4 → T1 ∥ T3 → T2（等数据链路）→ Phase 3 验证 → Phase 4 灰度。~~
**实际执行：P0 → 骨架 → T4/T1/T3/T2 → 验证 → 判定不采纳 → 归档。V7 基础设施保留，生产保持 V6。**
